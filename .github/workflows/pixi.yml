name: ci
on:
  workflow_dispatch:
  push:
    branches:
      - develop
  pull_request:
    branches:
      - develop
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: prefix-dev/setup-pixi@v0.8.0
    - run: pixi run test
